# 📅 Настройка автоматической ротации позиций

## Что это?

Каждую ночь система автоматически меняет позиции всех активных пакетов продвижения. Владельцы с пакетом "Золото" могут оказаться на 1-м месте, на 15-м или на 30-м — каждый день новая позиция в пределах их диапазона.

Для этого нужно настроить автоматический запуск функции каждый день в 00:00 по МСК.

---

## 🚀 Быстрая настройка (5 минут)

### Шаг 1: Добавьте секрет CRON_SECRET

1. Откройте генератор случайных строк: https://passwordsgenerator.net/
2. Настройте: длина 40 символов, только буквы и цифры
3. Скопируйте сгенерированную строку (например: `k8m2n4p6q8r0s2t4u6v8w0x2y4z6a8b0c2d4e6f8`)
4. В редакторе poehali.dev вставьте эту строку в форму "CRON_SECRET" (выше в чате)
5. Нажмите "Добавить"

### Шаг 2: Зарегистрируйтесь на cron-job.org

1. Откройте: https://cron-job.org/en/
2. Нажмите **Sign up** (регистрация бесплатная)
3. Введите email и пароль
4. Подтвердите email

### Шаг 3: Создайте задачу

1. После входа нажмите **Create cronjob**
2. Заполните форму:

**Title (название):**
```
Daily Rotation - Positions
```

**URL:**
```
https://functions.poehali.dev/34172fa3-9c6c-45b1-ad46-7e7d37c6a6e6
```

**Schedule (расписание):**
- Выберите: **Every day**
- Время: **00:00** (полночь по UTC, это 03:00 по МСК)

**Request method (метод):**
- Выберите: **POST**

**Request headers (заголовки):**
Нажмите **Add header** и добавьте:
```
Header name: Authorization
Header value: Bearer ВАШ_CRON_SECRET
```
⚠️ Замените `ВАШ_CRON_SECRET` на строку, которую вы создали в Шаге 1!

Пример:
```
Authorization: Bearer k8m2n4p6q8r0s2t4u6v8w0x2y4z6a8b0c2d4e6f8
```

3. Нажмите **Create cronjob**

---

## ✅ Проверка

### Тестовый запуск

1. В списке задач найдите вашу задачу
2. Нажмите **▶ Run** (запустить вручную)
3. Дождитесь результата (5-10 секунд)
4. Если статус **Success (200)** — всё работает! 🎉

### Что должно произойти

- **Status code:** 200
- **Response:** `{"success": true, "rotated": 0, ...}`

Если видите **401 Unauthorized** — проверьте, что CRON_SECRET добавлен правильно в заголовке.

---

## 🔧 Альтернативные сервисы

Если cron-job.org не подходит, используйте:

### EasyCron (https://easycron.com)

1. Регистрация бесплатная (до 1 задачи)
2. Create new cron job
3. URL: `https://functions.poehali.dev/34172fa3-9c6c-45b1-ad46-7e7d37c6a6e6`
4. Cron expression: `0 0 * * *` (каждый день в 00:00)
5. HTTP method: POST
6. HTTP headers: `Authorization: Bearer ВАШ_CRON_SECRET`

### cPanel (если у вас свой хостинг)

1. Откройте cPanel → Cron Jobs
2. Add new cron job
3. Minute: 0, Hour: 0, Day: *, Month: *, Weekday: *
4. Command:
```bash
curl -X POST -H "Authorization: Bearer ВАШ_CRON_SECRET" https://functions.poehali.dev/34172fa3-9c6c-45b1-ad46-7e7d37c6a6e6
```

---

## 📊 Мониторинг

### Как проверить, что ротация работает?

1. Войдите в личный кабинет владельца
2. Перейдите на вкладку **Продвижение**
3. Если у вас активный пакет, вы увидите текущую позицию
4. На следующий день позиция должна измениться

### Логи в cron-job.org

1. Откройте вашу задачу
2. Перейдите на вкладку **History**
3. Вы увидите все запуски с результатами

---

## ❓ Частые вопросы

**Q: Обязательно ли настраивать cron?**  
A: Нет, позиции пересчитываются автоматически при каждом просмотре каталога. Но cron делает систему более предсказуемой — позиции меняются ровно в полночь.

**Q: Можно ли запускать чаще, чем раз в день?**  
A: Да, но это бессмысленно — функция всё равно выдаст одну и ту же позицию до конца дня (по дизайну).

**Q: Что будет, если забыть настроить cron?**  
A: Ничего страшного — система продолжит работать, просто позиции будут обновляться "лениво" (при первом просмотре каталога после полуночи).

**Q: Можно ли изменить время запуска?**  
A: Да, но лучше оставить 00:00 UTC (03:00 МСК), чтобы новые позиции были готовы к утреннему трафику.

---

## 🛠 Техническая информация

- **Функция:** `/backend/daily-rotation/`
- **URL:** https://functions.poehali.dev/34172fa3-9c6c-45b1-ad46-7e7d37c6a6e6
- **Метод:** POST
- **Аутентификация:** Bearer token в заголовке `Authorization`
- **Секрет:** Хранится в переменной окружения `CRON_SECRET`
- **Логика:** Генерирует случайную позицию в диапазоне пакета на основе текущей даты

---

Готово! После настройки позиции будут автоматически обновляться каждую ночь. 🚀

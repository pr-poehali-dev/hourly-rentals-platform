# Система подписок и автоматической архивации

## Как работает система

### 1. Подписка объектов
- **Все новые объекты** автоматически получают 30 дней подписки при создании
- **Подписка контролируется** через админ-панель
- **Цены**: 2000₽/месяц для отелей, 1000₽/месяц для апартаментов

### 2. Контроль подписки в админ-панели

В карточке каждого объекта отображается:
- 🟢 **Зелёный бейдж** — более 7 дней до окончания
- ⚪ **Серый бейдж** — 4-7 дней до окончания
- 🔴 **Красный бейдж** — менее 3 дней или истекла
- ⏰ **Live-таймер** — точный обратный отсчёт (обновляется каждую секунду)

### 3. Установка подписки администратором

1. Нажмите на иконку **🕐 Часы** рядом с объектом
2. Введите количество дней или выберите быструю кнопку:
   - 30 дней
   - 90 дней (квартал)
   - 365 дней (год)
3. Нажмите **"Установить"**

**Важно**: Время накапливается! Если у объекта есть активная подписка, новое время добавится к существующему.

### 4. Автоматическая архивация

**Функция**: `cron-archive-expired`
**URL**: `https://functions.poehali.dev/43c17d19-e755-490d-b1e3-82394fdcf85e`

Эта функция автоматически архивирует объекты с истекшей подпиской.

#### Настройка автоматического запуска

Для ежедневной проверки используйте один из вариантов:

**Вариант 1: cron-job.org (рекомендуется)**
1. Зарегистрируйтесь на https://cron-job.org
2. Создайте новый cronjob:
   - URL: `https://functions.poehali.dev/43c17d19-e755-490d-b1e3-82394fdcf85e`
   - Method: POST
   - Schedule: Каждый день в 00:00 МСК
   - Timezone: Europe/Moscow

**Вариант 2: Сервер с crontab**
```bash
# Добавьте в crontab:
0 0 * * * curl -X POST https://functions.poehali.dev/43c17d19-e755-490d-b1e3-82394fdcf85e
```

**Вариант 3: GitHub Actions (если используете GitHub)**
Создайте файл `.github/workflows/archive-expired.yml`:
```yaml
name: Archive Expired Listings
on:
  schedule:
    - cron: '0 21 * * *'  # 00:00 MSK = 21:00 UTC
  workflow_dispatch:

jobs:
  archive:
    runs-on: ubuntu-latest
    steps:
      - name: Call archive function
        run: |
          curl -X POST https://functions.poehali.dev/43c17d19-e755-490d-b1e3-82394fdcf85e
```

### 5. Ручной запуск архивации

Если нужно немедленно архивировать истекшие объекты:

```bash
curl -X POST https://functions.poehali.dev/43c17d19-e755-490d-b1e3-82394fdcf85e
```

Ответ:
```json
{
  "success": true,
  "message": "Архивировано объектов: 3",
  "archived_count": 3,
  "archived_listings": ["Отель А", "Отель Б", "Апартаменты В"]
}
```

## Проверка системы

### SQL-запрос для проверки истекающих подписок:

```sql
SELECT id, title, city, 
       subscription_expires_at,
       EXTRACT(DAY FROM (subscription_expires_at - NOW())) as days_left
FROM listings
WHERE is_archived = FALSE
  AND subscription_expires_at IS NOT NULL
  AND subscription_expires_at < NOW() + INTERVAL '7 days'
ORDER BY subscription_expires_at ASC;
```

### Объекты с истекшей подпиской:

```sql
SELECT id, title, city, subscription_expires_at
FROM listings
WHERE is_archived = FALSE
  AND subscription_expires_at < NOW();
```

## Рекомендации

1. **Ежедневная проверка** — настройте автоматический запуск функции архивации
2. **Уведомления владельцев** — связывайтесь с владельцами за 3-7 дней до истечения
3. **Мониторинг** — проверяйте админ-панель на красные бейджи
4. **Продление заранее** — владельцы могут продлить подписку заранее, время накопится

## Текущий статус

Всем 18 активным объектам установлена подписка на 30 дней (до 12 февраля 2026).
